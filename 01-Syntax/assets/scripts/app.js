const defaultResult = 0;
let currentResult = defaultResult;

function add() {
  currentResult = currentResult + parseInt(userInput.value);
  outputResult(currentResult, "This works");
}

addBtn.addEventListener("click", add);

// One comment option

/*
This is also a comment 
*/

const myObject = {
  name: "Time",
  age: 23,
  website: nil,
};

typeof "max";
